type User {
  id: ID! @id
  name: String!
  email: String! @unique
  password: String!
  logEntries: [LogEntry!]!
}

type LogEntry {
  id: ID! @id
  createdAt: DateTime! @createdAt
  note: String!
  createdBy: User
}

